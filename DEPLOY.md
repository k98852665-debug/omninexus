# النشر المجاني على Render

1. ارفع النسخة المحدّثة كاملة إلى مستودع GitHub الخاص بالمشروع.
2. سجّل الدخول إلى https://dashboard.render.com.
3. اختر New → Blueprint ثم المستودع. ملف render.yaml يحدد خطة free صراحةً.
4. راجع الإعدادات، ثم أنشئ الخدمة وانتظر نجاح البناء.
5. افتح رابط onrender.com الذي تعطيه Render، واختبر المحادثة وتنزيل الملفات.

يمكن أيضًا اختيار New → Web Service، ربط المستودع، اختيار Docker وخطة Free، وتحديد /status لفحص الصحة.

المحادثة الفعلية تحتاج مفتاح Groq من حساب على خطة Free. أضفه كمتغير GROQ_API_KEY في إعدادات Environment لخدمة omninexus، ثم احفظ وأعد النشر. لا تضع المفتاح في المستودع. لا تفعّل إضافات مدفوعة. إذا طلبت الخدمة بطاقة أو ترقية، توقف لتظل ضمن شرط التشغيل المجاني.

خطة Free تتوقف بعد 15 دقيقة من الخمول، وقد تستغرق العودة حوالي دقيقة. سياق المحادثة المؤقت وملفات SQLite المحلية تُفقد عند الإيقاف أو إعادة التشغيل. الحفظ الدائم غير مهيأ في هذه النسخة.

المراجع الرسمية: https://render.com/docs/free وhttps://render.com/docs/blueprint-spec

## Docker محليًا

```sh
docker compose up --build
```

افتح http://localhost:8000. لا يوجد مشروع Android فعلي داخل المستودع الأصلي؛ buildozer.spec محفوظ كما هو، لكنه لا يضمن بناء APK.
